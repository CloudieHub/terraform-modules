# AWS VPC Module (CloudieHub)

Creates a VPC with public and private subnets, IGW, optional NAT Gateways, route tables and a sample bastion SG.

## Usage

See `examples/simple` for a root module that calls this module.
